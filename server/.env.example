# Server .env — copy from root and generate AGENT_TOKEN
# These should match the root .env.local values

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
OPENAI_API_KEY=
REDIS_URL=redis://localhost:6379
# Generate with: openssl rand -hex 32
AGENT_TOKEN=<generate-a-secure-random-token>
PORT=3001
CORTEX_DATA_DIR=./data/cortex
CORTEX_REFLEX_THRESHOLD=0.8
CORTEX_CONTEXT_MAX_LINES=150
BRIEFING_CRON=0 4 * * *
BRIEFING_CRON_TZ=UTC
PROACTIVE_ALERTS_CRON=*/30 * * * *
PROACTIVE_ALERTS_CRON_TZ=UTC
